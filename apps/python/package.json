{
  "name": "python",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python3 -m app.main",
    "build": "python3 -m compileall -q app",
    "lint": "python3 -m compileall -q app",
    "type-check": "python3 -m compileall -q app",
    "test": "python3 -m unittest discover -s tests -p 'test_*.py'"
  }
}