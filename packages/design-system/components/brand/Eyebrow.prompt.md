The signature mono, all-caps, wide-tracked label that sits above headings or labels a group.

```jsx
<Eyebrow>Client aftercare</Eyebrow>
<Eyebrow>Your products</Eyebrow>
```

- `tone`: `"clay"` (default) · `"ink"` · `"muted"`
- `as`: `"block"` (default) or `"inline"`
