A small mono-caps chip. Frosted variant floats over photos; clay and outline for solid contexts.

```jsx
<Tag>Morning</Tag>
<Tag variant="clay">New</Tag>
<Tag variant="outline" icon={<Favicon size={10} />}>Serum</Tag>
```
- `variant`: `"frost"` (default) · `"clay"` · `"outline"`
