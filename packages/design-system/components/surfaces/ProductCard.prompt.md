An aftercare product card, photo, step eyebrow, serif name, brand, price, quiet buy.

```jsx
<ProductCard
  image="/serum.jpg"
  step="Step 02 · Serum"
  name="Vitamin C15"
  brand="Medik8"
  price="£38"
  tag="New"
  onBuy={() => {}}
/>
```
Pass an `<image-slot>` as `image` for user-fillable photos. Omit `image` for a placeholder.
