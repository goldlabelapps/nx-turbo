A statement stat card, big serif-italic figure, supporting copy, optional mono source.

```jsx
<StatCard figure="3×" source="Internal pilot, 2025">
  clients follow their routine when it lives on their phone.
</StatCard>
```
- `variant` mirrors `Card` (`paper` / `glass` / `tile` / `ink`).
